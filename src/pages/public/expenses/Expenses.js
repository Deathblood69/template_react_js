import React from 'react'

/**
 * Composant
 * @returns {JSX.Element}
 * @constructor
 */
export const Expenses = () => {

  // ====== AFFICHAGE ====== //

  return <React.Fragment>
    <main style={{padding: "1rem 0"}}>
      <h2>Expenses</h2>
    </main>
  </React.Fragment>
}
